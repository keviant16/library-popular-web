import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.keviant.library',
  appName: 'library-popular-web',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
